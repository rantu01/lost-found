import LostFoundExplorer from '../../components/LostFoundExplorer'

export default function Page() {
  return (
    <LostFoundExplorer
      defaultStatus="FOUND"
      title="Found Items"
      description="Browse found reports and use the same responsive filters to narrow results on any device."
    />
  )
}